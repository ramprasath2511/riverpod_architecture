Implemented river_pod state management with good architecture


Getting Started
Run fvm install (FVM installation steps available here)
Run fvm flutter pub get
Run code generation: fvm flutter pub run build_runner build --delete-conflicting-outputs
Create an Imgur API key online
Set your PEXELS_API_KEY in launch.json
Run the app